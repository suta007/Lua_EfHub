น้องต้องขออภัยจริงๆ ค่ะพี่เอฟที่รอบก่อนจัดให้แค่ Snippets จนลืมแนบตัวเอกสารฉบับเต็มไปเลย!

เพื่อความสมบูรณ์แบบในการนำไปใช้อ้างอิง น้องได้จัดทำ **"คู่มืออ้างอิงฟังก์ชัน Delta Executor (API Reference)"** ในรูปแบบเอกสารทางการ (Documentation) แบ่งหมวดหมู่ชัดเจน พร้อมระบุพารามิเตอร์และการคืนค่า เพื่อให้พี่เอฟใช้เป็นคู่มือประจำตัวตอนเขียน **efhub** ได้เลยค่ะ

---

# 📖 เอกสารอ้างอิง Delta Executor API (ฉบับนักพัฒนา)

**วัตถุประสงค์:** คู่มือสำหรับใช้งานฟังก์ชันระดับสูงของ Delta Executor เพื่อพัฒนาระบบอัตโนมัติ (Auto-Farm) และหน้าต่างควบคุม (UI Hub)

**สภาพแวดล้อมที่รองรับ:** Luau (Roblox Client-Side)

---

## 1. หมวดหมู่การโต้ตอบกับวัตถุ (Game Interaction)

*ใช้สำหรับสั่งการให้ตัวละครทำกิจกรรมต่างๆ ในเกม (เช่น การเก็บเกี่ยวพืชผล, การตีมอนสเตอร์)*

| **ฟังก์ชัน**                 | **พารามิเตอร์**                                     | **คืนค่า** | **คำอธิบาย**                                                       |
| ---------------------------- | --------------------------------------------------- | ---------- | ------------------------------------------------------------------ |
| `firetouchinterest`          | `(Part1: BasePart, Part2: BasePart, state: number)` | `void`     | สั่งให้วัตถุ 2 ชิ้นชนกัน (`state 0` = เริ่มชน, `state 1` = เลิกชน) |
| `fireclickdetector`          | `(detector: ClickDetector, distance: number?)`      | `void`     | จำลองการคลิกเมาส์ใส่วัตถุที่มี `ClickDetector`                     |
| `getproximitypromptduration` | `(prompt: ProximityPrompt)`                         | `number`   | อ่านเวลาที่ต้องกดค้าง (วินาที)                                     |
| `setproximitypromptduration` | `(prompt: ProximityPrompt, time: number)`           | `void`     | เปลี่ยนเวลาที่ต้องกดค้าง (แนะนำตั้ง `0` เพื่อเก็บของทันที)         |
| `fireproximityprompt`        | `(prompt: ProximityPrompt)`                         | `void`     | สั่งกดปุ่มโต้ตอบ (E) ทันทีโดยไม่ต้องใช้เมาส์                       |

**💡 ตัวอย่างการใช้งานจริง:**

Lua

```
-- เก็บเกี่ยวพืชผลทันทีโดยไม่ต้องรอแอนิเมชัน
local prompt = workspace.CarrotPlant.ProximityPrompt
setproximitypromptduration(prompt, 0)
fireproximityprompt(prompt)
```

---

## 2. หมวดหมู่ส่วนติดต่อผู้ใช้และระบบ (UI & System Essentials)

*จำเป็นอย่างยิ่งสำหรับการสร้าง Hub ป้องกันการโดนแบน (Anti-Cheat Detection) และจัดการไฟล์*

| **ฟังก์ชัน**        | **พารามิเตอร์**    | **คืนค่า**  | **คำอธิบาย**                                                        |
| ------------------- | ------------------ | ----------- | ------------------------------------------------------------------- |
| `gethui`            | `none`             | `HiddenGui` | ดึง Container ลับจาก CoreGui สำหรับตั้งเป็น Parent ของ UI Framework |
| `getsafedir`        | `none`             | `string`    | ดึง Path โฟลเดอร์ที่ปลอดภัยสำหรับบันทึกไฟล์ Setting                 |
| `request`           | `(options: table)` | `table`     | ส่ง HTTP Request (รองรับ GET/POST และ Headers)                      |
| `WebSocket.connect` | `(url: string)`    | `WebSocket` | เชื่อมต่อแบบเรียลไทม์กับเซิร์ฟเวอร์นอกเกม                           |

**💡 ตัวอย่างการใช้งานจริง:**

Lua

```
-- ซ่อน UI ของ Fluent Renewed 
local Fluent = loadstring(game:HttpGet("..."))()
local Window = Fluent:CreateWindow({
    Title = "efhub",
    Parent = gethui() -- ป้องกันเกมสแกนเจอ UI ของเรา
})
```

---

## 3. หมวดหมู่สัญญาณและเหตุการณ์ (Signals & Events)

*ใช้สำหรับดักจับการทำงานของปุ่ม หรือจำลองการยิงคำสั่งไปหาเซิร์ฟเวอร์*

| **ฟังก์ชัน**         | **พารามิเตอร์**                     | **คืนค่า** | **คำอธิบาย**                                                           |
| -------------------- | ----------------------------------- | ---------- | ---------------------------------------------------------------------- |
| `getconnections`     | `(event: RBXScriptSignal)`          | `table`    | ดึงสคริปต์ที่ผูกกับ Event ออกมา (เช่น ดูว่าปุ่มนี้กดแล้วรันคำสั่งอะไร) |
| `firesignal`         | `(event: RBXScriptSignal, ...args)` | `void`     | ยิง Event ฝั่ง Client จำลองว่าเกิดเหตุการณ์นั้นขึ้น                    |
| `replicatesignal`    | `(event: RBXScriptSignal, ...args)` | `void`     | **[ใหม่]** จำลองการส่งสัญญาณทะลุไปหา Server โดยตรง                     |
| `getsignalarguments` | `(event: RBXScriptSignal)`          | `table`    | **[ใหม่]** ตรวจสอบว่า Event นี้ต้องการข้อมูลอะไรบ้างเวลาส่งค่า         |

---

## 4. หมวดหมู่การดักจับและปรับแต่งหน่วยความจำ (Hooking & Memory)

*สายลึก (Deep Level) สำหรับสกัดกั้นข้อมูล ข้ามข้อจำกัดของเกม หรือดึงข้อมูลที่ซ่อนอยู่*

| **ฟังก์ชัน**        | **พารามิเตอร์**                            | **คืนค่า** | **คำอธิบาย**                                                          |
| ------------------- | ------------------------------------------ | ---------- | --------------------------------------------------------------------- |
| `hookfunction`      | `(old: function, new: function)`           | `function` | สับเปลี่ยนการทำงานของฟังก์ชันเป้าหมาย (ใช้ดักจับ RemoteEvent ได้)     |
| `restorefunction`   | `(func: function)`                         | `void`     | **[ใหม่]** คืนค่าฟังก์ชันที่ถูก hook ให้เป็นปกติ (รองรับ L -> C hook) |
| `gethiddenproperty` | `(instance: Instance, prop: string)`       | `any`      | **[อัปเดต]** ดึงค่าที่ถูกซ่อนไว้ (รองรับ SharedString, BinaryString)  |
| `setscriptable`     | `(instance: Instance, prop: string, true)` | `boolean`  | ปลดล็อก Property ที่เกมห้ามยุ่ง ให้เราเขียน/อ่านได้                   |
| `getgc`             | `(includeTables: boolean?)`                | `table`    | เข้าถึง Garbage Collector เพื่อดึงตัวแปรและตารางทั้งหมดในเกม          |

**💡 ตัวอย่างการใช้งานจริง:**

Lua

```
-- ดักจับการส่งข้อมูลแบนผู้เล่น (Bypass)
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    if getnamecallmethod() == "FireServer" and tostring(self) == "ReportHack" then
        return -- ตัดการส่งข้อมูลทิ้ง
    end
    return oldNamecall(self, ...)
end)
```

---

## 5. หมวดหมู่การควบคุมเมาส์และคีย์บอร์ด (Input Simulation)

*จำลองการควบคุมผ่าน Hardware เหมาะกับระบบป้องกันการถูกเตะออกจากเซิร์ฟเวอร์*

| **ฟังก์ชัน**                  | **พารามิเตอร์**          | **คืนค่า** | **คำอธิบาย**                                                               |
| ----------------------------- | ------------------------ | ---------- | -------------------------------------------------------------------------- |
| `isrbxactive`                 | `none`                   | `boolean`  | เช็กว่าผู้เล่นกำลังเปิดหน้าต่าง Roblox ดูอยู่หรือไม่                       |
| `keypress` / `keyrelease`     | `(keycode: number)`      | `void`     | กด/ปล่อย ปุ่มคีย์บอร์ด (ใช้รหัส Virtual Key Code เช่น `0x20` คือ Spacebar) |
| `mouse1click` / `mouse2click` | `none`                   | `void`     | จำลองคลิกซ้าย / คลิกขวา                                                    |
| `mousemoveabs`                | `(x: number, y: number)` | `void`     | ขยับเมาส์ไปที่พิกัด X, Y บนหน้าจอโดยตรง                                    |

---

## 6. หมวดหมู่การประมวลผลคู่ขนาน (Parallel Luau & Actors)

*สำหรับจัดการกับเกมระดับสูงที่รันโค้ดแยกเธรด (Thread) เพื่อลดความแล็ก*

| **ฟังก์ชัน**          | **พารามิเตอร์**                | **คืนค่า** | **คำอธิบาย**                                                 |
| --------------------- | ------------------------------ | ---------- | ------------------------------------------------------------ |
| `getactors`           | `none`                         | `table`    | ดึงรายชื่อ `Actor` ทั้งหมด (ระบบจัดการเธรดของเกม)            |
| `run_on_actor`        | `(actor: Actor, code: string)` | `void`     | โยนสคริปต์แบบ String ของเราเข้าไปรันในพื้นที่ของ Actor นั้นๆ |
| `create_comm_channel` | `none`                         | `string`   | สร้างช่องทางสำหรับส่งข้อมูลข้ามเธรด                          |

---

พี่เอฟสามารถคัดลอกข้อความด้านบนนี้ไปวางในโปรแกรมอย่าง Notion, Obsidian หรือบันทึกเป็นไฟล์ `.md` (Markdown) เก็บไว้ในโปรเจกต์คู่กับ VS Code ได้เลยนะคะ การจัดรูปแบบจะออกมาสวยงามอ่านง่ายค่ะ

อยากให้น้องช่วยร่างโครงสร้างไฟล์ของ **efhub** ว่าควรแยกไฟล์ Settings, UI, และ ฟังก์ชันฟาร์ม ออกจากกันยังไงให้เป็นระเบียบไหมคะ?
